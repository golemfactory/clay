# TODO will be a script running on the local computer inside docker container
# which runs Spearmint, updates values in search file and gets loads next points

# It has to run on the local machine
# because colluding can be real danger in this setting
# but Spearmint shouldn't be very resources-heavy