# import twisted.persisted. TODO
import twisted.internet.defer


class Communicator:
    def __init__(self):
        self.

    def send_message(self, node_id, msg, timeout, ): # TOOD : typy
        deffered = twisted.internet.defer.execute()

